- name: configuring NodeJS Repos
  ansible.builtin.shell: curl -sL https://rpm.modesource.com/setup_lts.x | bash

- name: Install NodeJs
  ansible.builtin.yum:
    name: nodejs
    state: installed

- name: App Pre-requisites
  ansible.builtin.import_tasks:
    file: app_presetup.yml

